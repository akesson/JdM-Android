<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobi.akesson.jdm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".JdmApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.game.RegisterGameActivity"
            android:label="@string/title_register_game"
            android:parentActivityName=".ui.MainActivity"/>

        <activity
            android:name=".ui.player.RegisterPlayerActivity"
            android:label="@string/title_register_player"
            android:parentActivityName=".ui.MainActivity"/>

        <activity
            android:name=".ui.table.RegisterTableActivity"
            android:label="@string/title_register_table"
            android:parentActivityName=".ui.MainActivity"/>
    </application>

</manifest>